define(['skeleton'], function(sk) {
    var CollectionState = sk.Model.extend({
        name: 'CollectionState',
        configure: function(){
        }
    });
    return CollectionState;
});